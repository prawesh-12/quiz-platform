{
  "name": "quizloom",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm install --prefix services/auth && npm install --prefix services/questionbank && npm install --prefix services/quiz && npm install --prefix services/exam && npm install --prefix services/analytics && npm install --prefix services/scheduler && npm install --prefix client",
    "dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d && npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "up": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
    "down": "docker compose down",
    "reset": "bash scripts/reset.sh",
    "test": "node --test tests/unit/*.test.js tests/events/*.test.js tests/integration/*.test.js",
    "up:prod": "docker compose up --build",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js",
    "test:events": "node --test tests/events/*.test.js"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "eslint-plugin-react-hooks": "^5.1.0",
    "globals": "^15.14.0"
  }
}
