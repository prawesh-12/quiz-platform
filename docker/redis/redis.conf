# Cache-only Redis: data is disposable (app falls back to PostgreSQL).

bind 0.0.0.0
protected-mode no
port 6379

# Bound memory, evict LRU under pressure.
maxmemory 256mb
maxmemory-policy allkeys-lru

# No persistence — it's a cache.
save ""
appendonly no

timeout 300
tcp-keepalive 60
